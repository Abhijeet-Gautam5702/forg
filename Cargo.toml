[package]
name = "forg"
version = "2.0.1"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
regex = "1.12.3"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
assert_cmd = "2.0"
tempfile = "3.8"
predicates = "3.0"
